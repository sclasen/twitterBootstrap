# Application dependencies

require:
    - play
    - play -> less 0.3
    - localModule -> twitterBootstrap 1.2.0 

repositories:
    - localModule:
        type: local
        artifact: "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - localModule -> *
