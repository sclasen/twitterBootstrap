# Application dependencies

require:
    - play
    - play -> less 0.3
    - localModule -> twitter_bootstrap 1.2.0 

repositories:
    - localModule:
        type: local
        artifact: "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - localModule -> *
